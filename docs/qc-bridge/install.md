# Установка — QC Bridge Maya ↔ Blender

Ставится один раз, дальше Blender сам предлагает обновления.

## Что понадобится

Blender **4.2** или новее. Версию видно в заголовке окна Blender, а также
в `Help → About Blender`.

## Шаг 1. Добавить репозиторий

Это делается **один раз на машину**. Если вы уже добавляли репозиторий для
другого аддона Mutaform, всё равно добавьте — у каждого аддона он свой.

1. `Edit → Preferences…` — откроются настройки Blender
2. Слева выбрать раздел **Get Extensions**
3. В правом верхнем углу нажать значок шестерёнки → **Repositories**
4. В открывшемся окне нажать **+** → **Add Remote Repository**
5. В поле **URL** вставить ссылку:

    ```text
    https://mutaform.github.io/qc-bridge-blender-maya/index.json
    ```

6. Поставить галочку **Check for Updates on Startup**
7. Нажать **Create**

Окно репозиториев можно закрыть.

![Как добавить репозиторий](../img/install-extension.gif){ .screenshot }

На записи показан пример с одним из аддонов — порядок кнопок для всех
одинаковый, отличается только ссылка. Ваша ссылка — та, что в шаге 5 выше.

## Шаг 2. Установить аддон

1. Оставаясь в **Get Extensions**, в строке поиска сверху набрать:

    ```text
    QC Bridge
    ```

2. В списке появится аддон — нажать **Install** справа от его названия

Всё. Настройки Blender можно закрывать.

## Шаг 3. Проверить, что получилось

В **шапке 3D-вьюпорта** появилась синяя иконка Maya — сразу после кнопок
Proportional Editing. По клику раскрывается меню моста.

![Кнопка моста в шапке вьюпорта](../qc-bridge/img/header-button.png){ .screenshot }

!!! note "Вкладки в N-панели больше нет"
    До версии 1.2.0 мост жил вкладкой **QC Maya Bridge** в боковой панели.
    Если вы обновились со старой версии и ищете её — она убрана намеренно,
    всё содержимое переехало в это меню.

Если панель на месте — установка прошла.

## Обновления

Blender проверяет их сам при запуске, потому что на шаге 1 включена галочка
**Check for Updates on Startup**. Когда выйдет новая версия, она появится в
`Get Extensions` с кнопкой **Update**.

Проверить вручную: `Get Extensions` → шестерёнка → **Check for Updates**.

Версия установленного аддона написана в заголовке его панели справа, например
`ver 1.0.0`. Она же нужна, чтобы понять, ту ли документацию вы читаете —
переключатель версий стоит в шапке этого сайта.

## Если не получилось

| Что видно | В чём дело |
| --- | --- |
| В поиске ничего не находится | Репозиторий не добавился или ссылка вставлена с опечаткой. Вернитесь на шаг 1 и проверьте адрес целиком |
| Аддон в списке есть, но `Install` не нажимается | Ваш Blender старее, чем **4.2**. Обновите Blender |
| Установился, но кнопки нет | Мост живёт в **шапке 3D-вьюпорта**, а не в боковой панели: синяя иконка Maya сразу после кнопок Proportional Editing. Если шапка узкая, часть кнопок в ней скрыта — потяните её колесом мыши |
| Список репозиториев пуст | Нет доступа в сеть или он закрыт прокси. Тогда попросите у студии архив и поставьте из файла: `Get Extensions` → значок `▼` справа сверху → **Install from Disk…** |

## Вторая половина — в Maya

Мост состоит из двух частей, и **нужны обе**. Шаги выше поставили половину в
Blender; теперь Maya. Ставится перетаскиванием одного файла.

1. Скачать [`mutaform_bridge_maya.zip`](https://mutaform.github.io/qc-bridge-blender-maya/mutaform_bridge_maya.zip)
2. Распаковать **туда, где папка останется навсегда**: в папку инструментов, на
   сетевую шару — куда угодно постоянное
3. Запустить Maya и перетащить `install/install.py` из распакованной папки
   **мышью прямо во вьюпорт**

Установщик пропишет модуль Maya и добавит кнопку **QC Bridge** на полку
*Mutaform*. Делается один раз: дальше Maya подхватывает мост при каждом запуске.

!!! warning "Куда распакуете — там и живёт"
    Распакованная папка **и есть установка**: Maya получает лишь указатель на
    неё. Перенесёте или удалите папку — мост отвалится, тогда распакуйте заново
    и перетащите `install.py` ещё раз.

!!! danger "Если мост уже стоял — версии 1.1.x"
    Раньше Maya-половина ставилась иначе: папка копировалась в `scripts\`, а
    кнопка заводилась кодом из Script Editor. Установщик 1.2.0 убирает это сам —
    снимает старые кнопки с полок, удаляет старую копию из `scripts\` и
    вычищает её из памяти Maya.

    Отдельно ничего делать не нужно, но знать стоит: **кнопка переехала**. Была
    на полке *Polygons*, стала на *Mutaform* — там же, где QC Bake и валидатор.

## Обновления Maya-половины

Мост проверяет их сам при каждом открытии панели. Если вышла версия новее,
сверху появляется полоса с кнопками **Install** и **Skip**. По **Install**
архив скачивается, сверяется с контрольной суммой и подменяет папку — без
перезапуска Maya. Прежняя версия хранится, пока новая не загрузится.

Выключить автопроверку или запустить её вручную — **Settings → Updates** в
панели моста.

!!! warning "Обе стороны должны смотреть в одну папку"
    Главное условие работы моста: **папка обмена в Blender и в Maya одна и та
    же**. Если передача не срабатывает, проверьте это первым делом.

## Дальше

Что аддон умеет — в [обзоре](index.md). Разбор каждой кнопки — в
[справочнике](reference.md).
